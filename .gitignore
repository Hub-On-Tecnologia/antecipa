node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
firebase-applet-config.json
firebase-service-account*.json
*-key.json
*.pem
.vps-config
crítica/
# Transcrições de sessão do IDE. Não são código, e já carregaram token de
# webhook do Bitrix colado durante uma conversa — o que virou segredo commitado.
.antigravity/
